<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Custom Permissions -->
    <permission
        android:name="com.example.apilessonapp.WRITE_DATABASE"
        android:protectionLevel="signature" />
    <permission
        android:name="com.example.apilessonapp.READ_DATABASE"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestProgram"
        tools:targetApi="31">
        <provider
            android:name=".contentprovider.StudentProvider"
            android:authorities="com.example.apilessonapp"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="true"
            android:readPermission="com.example.apilessonapp.READ_DATABASE"
            android:writePermission="com.example.apilessonapp.WRITE_DATABASE" />


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TestProgram">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PaintingActivity" />
        <activity android:name=".AnimateActivity" />
    </application>

</manifest>